export type Props = {
    children: React.ReactNode
}

export type loginProp={
    email:string,
    password:string
}

export type signUpProps={
    first_name:string,
    last_name:string,
    email:string,
    password:string
}